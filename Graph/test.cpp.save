#include <TXLib.h>
#include "APLib.h"

struct Points
{
    int x, y, r;
    COLORREF color;
};

int main()
{
    txCreateWindow(800, 600);

    while(!KEY(VK_ESCAPE))
    {
        Points p = {100, 200, 100, RGB(random), RGB};

        apCircle(p.x, p.y, p.r, p.color);
    }
    
    return 0;
}
