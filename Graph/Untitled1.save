#include <TXLib.h>

//--------------------------------------------------------------------------------------------

class Pos
{
    private:

    struct Pash
    {
        int xMin, xMax, yMin, yMax, mode;
    };

    struct Hoope
    {
        int x, y, mode;
    };

    void Scaner(/*Hoope* t*/);
};

//--------------------------------------------------------------------------------------------

void Scaner(/*Hoope* t*/)
{
    t -> x = txMouseX();
    t -> y = txMouseY();
    t -> mode = txMouseButtons();
}
